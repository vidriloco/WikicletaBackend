<div class="control-group">	
	<%= f.label :lock_used, t('incidents.views.new.form.lock_used'), :class => "control-label" %>
	<div class="controls">
		<%= f.select :lock_used, options_for_select(Bike.humanized_categories_for(:locks).invert, incident.lock_used) %>
	</div>
</div>